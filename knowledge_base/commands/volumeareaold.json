{
  "name": "volumeareaold",
  "syntax": "/volumeareaold add <identifier: string> <from: x y z> <to: x y z> [name: string] | /volumeareaold remove <name: string> | /volumeareaold remove_all | /volumeareaold list",
  "description": "旧版体积区域管理命令",
  "category": "world_management",
  "parameters": [
    {
      "name": "identifier",
      "type": "string",
      "required": true,
      "description": "体积区域标识符"
    },
    {
      "name": "from",
      "type": "x y z",
      "required": true,
      "description": "区域起始坐标"
    },
    {
      "name": "to",
      "type": "x y z",
      "required": true,
      "description": "区域结束坐标"
    },
    {
      "name": "name",
      "type": "string",
      "required": false,
      "description": "区域名称"
    }
  ],
  "examples": [
    {
      "input": "/volumeareaold add minecraft:fog_default 0 0 0 100 256 100",
      "description": "旧版语法添加体积区域"
    }
  ],
  "bedrock_specific_notes": "volumeareaold是volumearea命令的旧版保留。建议使用volumearea命令。",
  "version_added": "1.18.0"
}